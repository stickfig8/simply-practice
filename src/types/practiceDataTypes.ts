export type PracticeLog = {
  date: string;
  songTitle: string;
  instrument: string;
  practicePart: string;
  bpm: number;
  desc: string;
};

export type PracticeWeekData = {
  week: string;
  count: number;
};
